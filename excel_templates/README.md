# Шаблоны Excel

Каталог для исходных шаблонов `.xlsx`. Отчёты генерируются программно через `src/excel/report_builder.py` и сохраняются в `reports/weekly/`.

Шаблоны создаются при первом запуске `make report` или DAG `weekly_market_report`.
